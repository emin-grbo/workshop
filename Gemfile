source "https://rubygems.org"

# This is the GitHub Pages gem, which includes all dependencies required to run
# GitHub Pages locally. It includes Jekyll and several useful plugins.
gem "github-pages", group: :jekyll_plugins